{
  "name": "the-great-fall",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "solve": "tsx scripts/solve-levels.ts",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.18.2",
    "lucide": "^0.468.0",
    "playcanvas": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
